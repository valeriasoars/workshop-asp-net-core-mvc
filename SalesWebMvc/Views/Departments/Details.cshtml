@model SalesWebMvc.Models.Department

@{
    ViewData["Title"] = "Details";
}

<h1>@ViewData["Title"]</h1>
<h3>Details seller's @Model.Name</h3>

<div>
    <h4>Department</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary rounded">Edit</a> |
    <a asp-action="Index" class="btn btn-outline-primary rounded">Back to List</a>
</div>
